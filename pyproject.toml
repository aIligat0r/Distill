[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "distill"
version = "0.1.0"

[tool.setuptools.packages.find]
where = [".", "src", "cli"]
exclude = ["tests*", "assets", "scripts"]

[project.scripts]
distill = "cli.cli:main"